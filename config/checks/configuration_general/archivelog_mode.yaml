check_id: archivelog_mode
group_id: configuration_general
title: Base de datos en modo ARCHIVELOG
severity: FAIL
collector:
  type: inventory
  source: database
  field: archivelog_mode
evaluator:
  type: expected_value
  expected: ARCHIVELOG
applies_to:
  architectures: [standalone, rac, dataguard, rac_dataguard]
  open_modes: [READ WRITE, READ ONLY, READ ONLY WITH APPLY]
  requires:
    database_connection: true
remediation:
  summary: Habilitar ARCHIVELOG si los requisitos de recuperabilidad lo demandan.
  actions: ["Planificar una ventana si aplica y habilitar el modo ARCHIVELOG."]
  owner: DBA
  requires_window: true
  outage_risk: high
tags: [oracle, backup]
