check_id: performance_long_operations_active
group_id: performance
title: Operaciones largas activas
description: Performance básico actual con vistas dinámicas permitidas; fuente V$SESSION_LONGOPS.
severity: WARNING
collector:
  type: performance
  source_view: V$SESSION_LONGOPS
  max_rows_policy: performance_default_max_rows
evaluator:
  type: performance
  metric: performance_long_operations_active
  warning_count_policy: performance_longops_warning_count
  warning_time_remaining_seconds_policy: performance_longops_warning_time_remaining_seconds
applies_to:
  architectures: [standalone, rac, dataguard, rac_dataguard]
  open_modes: [READ WRITE, READ ONLY, READ ONLY WITH APPLY]
  requires:
    database_connection: true
remediation:
  summary: Revisar evidencia actual y correlacionar con actividad de aplicación antes de concluir causa raíz.
  actions:
    - Interpretar la evidencia como fotografía actual, no como histórico ni DB Time.
    - Revisar wait_class, event, SQL_ID, módulo, máquina y sesiones asociadas.
    - Validar si el patrón persiste con herramientas permitidas y licenciamiento aprobado.
  owner: DBA
  requires_window: false
  outage_risk: low
tags: [oracle, performance, actual, sin_awr, sin_ash]
