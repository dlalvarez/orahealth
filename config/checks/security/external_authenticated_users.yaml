check_id: external_authenticated_users
group_id: security
title: Usuarios con autenticación externa
severity: INFO
collector:
  type: oracle_security
  field: external_authenticated_users
  label: Usuarios no internos con autenticación externa configurada
  source_view: dba_users
  missing_status: SKIPPED
evaluator:
  type: oracle_security
  label: Usuarios no internos con autenticación externa configurada
  failure_status: INFO
  collection_error_status: SKIPPED
applies_to:
  architectures: [standalone, rac, dataguard, rac_dataguard]
  open_modes: [READ WRITE, READ ONLY, READ ONLY WITH APPLY]
  requires:
    database_connection: true
remediation:
  summary: Validar que la autenticación externa sea intencional y revisar controles de directorio o sistema operativo.
  actions:
    - Revisar la evidencia recolectada y confirmar si corresponde a una excepción aprobada.
    - Aplicar el principio de menor privilegio antes de mantener la configuración.
    - Documentar responsable, justificación y vigencia de cualquier excepción.
  owner: DBA
  requires_window: false
  outage_risk: low
tags: [oracle, seguridad, fase_3b]
