<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>OraHealthCheck Reporte Técnico - {{ target.target_id }}</title>
  <style>
    :root{--bg:#f4f7fb;--surface:#fff;--text:#172033;--muted:#637083;--line:#dfe7f1;--shadow:0 10px 30px rgba(23,32,51,.08);--pass:#15803d;--pass-bg:#dcfce7;--info:#1d4ed8;--info-bg:#dbeafe;--warning:#b45309;--warning-bg:#fef3c7;--fail:#dc2626;--fail-bg:#fee2e2;--critical:#7f1d1d;--critical-bg:#fecaca;--error:#581c87;--error-bg:#f3e8ff;--skipped:#64748b;--skipped-bg:#f1f5f9}
    *{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Arial,sans-serif;line-height:1.45}.page{max-width:1280px;margin:0 auto;padding:32px 20px}.hero{background:linear-gradient(135deg,#13213a,#234876);color:#fff;border-radius:24px;padding:30px;box-shadow:var(--shadow)}.eyebrow{text-transform:uppercase;letter-spacing:.12em;font-size:.78rem;opacity:.78}.hero h1{margin:.25rem 0;font-size:2rem}.hero-grid,.cards,.inventory-grid,.info-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:14px}.hero-grid{margin-top:22px}.hero-item{background:rgba(255,255,255,.12);border:1px solid rgba(255,255,255,.18);border-radius:16px;padding:12px}.hero-item span,.metric .label,.info-item span{display:block;color:inherit;opacity:.72;font-size:.76rem}.section{background:var(--surface);border:1px solid var(--line);border-radius:22px;box-shadow:var(--shadow);padding:24px;margin-top:22px}.section h2{margin:0 0 8px}.hint{color:var(--muted);font-size:.94rem;margin-top:0}.card,.metric,.info-item{background:#fff;border:1px solid var(--line);border-radius:18px;padding:16px;box-shadow:0 4px 16px rgba(23,32,51,.04)}.card .label{color:var(--muted);font-size:.84rem}.card .value{font-size:1.8rem;font-weight:800}.metric strong,.info-item strong{display:block;margin-top:4px;word-break:break-word}.badge{display:inline-flex;align-items:center;gap:6px;border-radius:999px;padding:6px 11px;font-weight:700;font-size:.78rem;white-space:nowrap;border:1px solid transparent}.badge.PASS{color:var(--pass);background:var(--pass-bg);border-color:#bbf7d0}.badge.INFO{color:var(--info);background:var(--info-bg);border-color:#bfdbfe}.badge.WARNING{color:var(--warning);background:var(--warning-bg);border-color:#fde68a}.badge.FAIL{color:var(--fail);background:var(--fail-bg);border-color:#fecaca}.badge.CRITICAL{color:var(--critical);background:var(--critical-bg);border-color:#fca5a5}.badge.ERROR{color:var(--error);background:var(--error-bg);border-color:#e9d5ff}.badge.SKIPPED{color:var(--skipped);background:var(--skipped-bg);border-color:#e2e8f0}.table-wrap{overflow-x:auto;border:1px solid var(--line);border-radius:16px;margin-top:14px}table{width:100%;border-collapse:collapse;background:#fff}th,td{padding:12px 13px;text-align:left;border-bottom:1px solid var(--line);vertical-align:top}th{background:#f8fafc;color:#475569;font-size:.76rem;text-transform:uppercase;letter-spacing:.06em}tr:last-child td{border-bottom:0}.group-title{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:20px;padding:14px 16px;border-radius:16px;background:#eef4ff;border:1px solid #d8e5ff}.group-title h3{margin:0}.evidence{max-width:520px}.json-block{background:#0f172a;color:#e2e8f0;border-radius:14px;padding:14px;max-height:260px;overflow:auto;white-space:pre-wrap;word-break:break-word;font-size:.82rem}.message{min-width:220px}.muted{color:var(--muted)}@media(max-width:700px){.page{padding:18px 12px}.hero{padding:22px}th,td{padding:10px}.json-block{max-width:72vw}}
  </style>
</head>
<body>
{% macro badge(status) -%}
  <span class="badge {{ status }}">{% if status == 'PASS' %}🟢{% elif status == 'INFO' %}🔵{% elif status == 'WARNING' %}🟡{% elif status == 'FAIL' %}🔴{% elif status == 'CRITICAL' %}🛑{% elif status == 'ERROR' %}🟣{% else %}⚪{% endif %} {{ status }} / {{ status | status_label }}</span>
{%- endmacro %}
<div class="page">
  <header class="hero"><div class="eyebrow">OraHealthCheck</div><h1>Reporte Técnico</h1><p>Evidencia técnica completa para equipos DBA, sistema operativo, seguridad y administración.</p><div class="hero-grid"><div class="hero-item"><span>ID del target</span><strong>{{ target.target_id }}</strong></div><div class="hero-item"><span>Nombre del target</span><strong>{{ target.name }}</strong></div><div class="hero-item"><span>Ambiente</span><strong>{{ target.environment }}</strong></div><div class="hero-item"><span>Arquitectura esperada</span><strong>{{ target.expected_architecture }}</strong></div><div class="hero-item"><span>Perfil</span><strong>{{ target.profile }}</strong></div><div class="hero-item"><span>Fecha/hora de ejecución</span><strong>{{ generated_at }}</strong></div></div></header>

  <section class="section"><h2>Información del target</h2><h3>Datos generales</h3><div class="info-grid">{% for item in target_info.general %}<div class="info-item"><span>{{ item.label }}</span><strong>{{ item.value }}</strong></div>{% endfor %}</div>{% if target_info.database %}<h3>Datos de base de datos</h3><div class="info-grid">{% for item in target_info.database %}<div class="info-item"><span>{{ item.label }}</span><strong>{{ item.value }}</strong></div>{% endfor %}</div>{% endif %}{% if target_info.operating_system %}<h3>Datos del servidor / sistema operativo</h3><div class="info-grid">{% for item in target_info.operating_system %}<div class="info-item"><span>{{ item.label }}</span><strong>{{ item.value }}</strong></div>{% endfor %}</div>{% endif %}</section>

  <section class="section"><h2>Resumen Global</h2><p class="hint">SKIPPED / Omitido significa que la validación no se ejecutó porque no cumplía aplicabilidad, configuración o fuentes de datos requeridas. failure_severity indica la severidad configurada cuando una validación detecta incumplimiento.</p><div class="cards"><div class="card"><div class="label">Puntaje de Salud</div><div class="value">{{ summary.score }}</div></div><div class="card"><div class="label">Estado Global</div><div class="value" style="font-size:1.1rem">{{ badge(summary.global_status) }}</div></div><div class="card"><div class="label">Total de validaciones</div><div class="value">{{ summary.total_checks }}</div></div><div class="card"><div class="label">PASS</div><div class="value">{{ summary.PASS }}</div></div><div class="card"><div class="label">INFO</div><div class="value">{{ summary.INFO }}</div></div><div class="card"><div class="label">WARNING</div><div class="value">{{ summary.WARNING }}</div></div><div class="card"><div class="label">FAIL</div><div class="value">{{ summary.FAIL }}</div></div><div class="card"><div class="label">CRITICAL</div><div class="value">{{ summary.CRITICAL }}</div></div><div class="card"><div class="label">ERROR</div><div class="value">{{ summary.ERROR }}</div></div><div class="card"><div class="label">SKIPPED</div><div class="value">{{ summary.SKIPPED }}</div></div></div></section>

  <section class="section">
    <h2>Inventario Técnico</h2>
    <h3>Inventario de Base de Datos</h3>
    <div class="inventory-grid">{% for item in database_inventory_items %}<div class="metric"><span class="label">{{ item.label }}</span><strong>{{ item.value }}</strong><small class="muted">{{ item.technical_name }}</small></div>{% endfor %}</div>
    <h3>Inventario del Sistema Operativo</h3>
    <div class="inventory-grid"><div class="metric"><span class="label">Plataforma</span><strong>{{ os_inventory_enriched.platform }}</strong></div>{% if os_inventory_enriched.distribution %}<div class="metric"><span class="label">Distribución</span><strong>{{ os_inventory_enriched.distribution }}</strong></div>{% endif %}<div class="metric"><span class="label">CPU</span><strong>{{ os_inventory_enriched.cpu_summary }}</strong></div><div class="metric"><span class="label">Memoria</span><strong>{{ os_inventory_enriched.memory_summary }}</strong></div><div class="metric"><span class="label">Filesystems</span><strong>{{ os_inventory_enriched.filesystems_summary }}</strong></div></div>
    {% if os_inventory_enriched.filesystems %}<div class="table-wrap"><table><thead><tr><th>Filesystem</th><th>Mount</th><th>Uso (%)</th></tr></thead><tbody>{% for fs in os_inventory_enriched.filesystems %}<tr><td>{{ fs.filesystem }}</td><td>{{ fs.mount }}</td><td>{{ fs.used_pct }}</td></tr>{% endfor %}</tbody></table></div>{% endif %}
  </section>

  <section class="section">
    <h2>Detalle de validaciones</h2><p class="hint">Las validaciones se agrupan por nombre funcional. La evidencia se muestra como JSON formateado y acotado para evitar que payloads extensos rompan el diseño.</p>
    {% for group_id, group_results in grouped_results.items() %}<div class="group-title"><h3>{{ group_id | group_label }}</h3><span>{{ group_results|length }} validación(es) · <small>{{ group_id }}</small></span></div><div class="table-wrap"><table><thead><tr><th>check_id</th><th>Título</th><th>Grupo</th><th>Estado</th><th>failure_severity</th><th>Mensaje</th><th>Evidencia</th><th>skipped_reason</th><th>error</th><th>duration_ms</th></tr></thead><tbody>{% for result in group_results %}<tr><td><strong>{{ result.check_id }}</strong></td><td>{{ result.title | check_title }}</td><td>{{ result.group_id | group_label }}<br><small>{{ result.group_id }}</small></td><td>{{ badge(result.status.value) }}</td><td>{{ result.failure_severity }}</td><td class="message">{{ result.message | friendly_message }}</td><td class="evidence"><pre class="json-block">{{ result.evidence | pretty_json }}</pre></td><td class="muted">{{ result.skipped_reason | friendly_message }}</td><td class="muted">{{ result.error or '' }}</td><td>{{ result.duration_ms }}</td></tr>{% endfor %}</tbody></table></div>{% endfor %}
  </section>
</div>
</body>
</html>
